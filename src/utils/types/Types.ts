type ProjectType = {
  name: string,
  duration: string,
  category: string,
  imageSrc: string,
  preview: string
}

type StackType = {
  imageSrc: string;
  title: string;
  subtitle: string;
  text: string;
};

export type {ProjectType, StackType}